language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-templates' ]; then cd .. && eval "mv $currentfolder generator-react-templates" && cd generator-react-templates; fi
